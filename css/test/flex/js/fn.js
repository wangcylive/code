/**
 * Created by Wangcy on 2014/11/4.
 */
(function(w, d) {
    var test01 = d.querySelector(".test-01");
    var display = document.defaultView.getComputedStyle(test01, null).display;

    //var isSupports = CSS.supports("(display:flex) or (display:-webkit-flex) or (display:flexbox) or (display:-webkit-flexbox)");
}(window, document));